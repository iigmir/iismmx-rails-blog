<%= form_for @article do |m| %>
<main>
  <table>
    <tr>
      <td> <%= m.label :title %> </td>
      <td> <%= m.text_field :title %> </td>
    </tr>
    <tr>
      <td> <%= m.label :context %> </td>
      <td> <%= m.text_area :context %> </td>
    </tr>
    <tr>
      <td> <%= m.submit %> </td>
      <td></td>
    </tr>
  </table>
</main>
<% end %>
